---
phase: 04-hierarchy-layout-detection
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/bin/lib/init.cjs
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "`init phase-op` no longer crashes when resolving fallback roadmap phase metadata."
    - "Init read-path workflows preserve resolver-backed fallback behavior and legacy output contracts."
    - "Init test coverage proves fallback behavior remains functional after the fix."
  artifacts:
    - path: "get-shit-done/bin/lib/init.cjs"
      provides: "Resolver-backed init phase-op fallback wiring with imported roadmap helper"
      contains: "getRoadmapPhaseInternal"
    - path: "tests/init.test.cjs"
      provides: "Regression coverage for init phase-op fallback behavior"
      contains: "cmdInitPhaseOp fallback"
  key_links:
    - from: "get-shit-done/bin/lib/init.cjs"
      to: "get-shit-done/bin/lib/core.cjs"
      via: "module destructure imports `getRoadmapPhaseInternal` used by `cmdInitPhaseOp`"
      pattern: "getRoadmapPhaseInternal"
    - from: "tests/init.test.cjs"
      to: "get-shit-done/bin/lib/init.cjs"
      via: "fallback suite executes init phase-op path"
      pattern: "cmdInitPhaseOp fallback"
---

<objective>
Close the Phase 4 verification blocker by restoring missing wiring in init phase-op fallback roadmap lookup.

Purpose: Recover full resolver-backed init read-path reliability so Phase 4 meets all must-haves.
Output: One targeted init import fix and green init fallback regression tests.
</objective>

<execution_context>
@/Users/lupickup/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/lupickup/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-hierarchy-layout-detection/04-hierarchy-layout-detection-VERIFICATION.md
@.planning/phases/04-hierarchy-layout-detection/04-02-SUMMARY.md
@get-shit-done/bin/lib/init.cjs
@tests/init.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore init phase-op roadmap fallback import wiring</name>
  <files>get-shit-done/bin/lib/init.cjs</files>
  <action>Update the top-level destructured import from `./core.cjs` to include `getRoadmapPhaseInternal`, then verify the existing `cmdInitPhaseOp` fallback call path uses that imported symbol without altering output field names (`roadmap_path`, `roadmap_exists`, `phase_path`, `phase_exists`) or resolver metadata keys. Keep this fix minimal and scoped to missing wiring identified in verification.</action>
  <verify>npm test -- tests/init.test.cjs --grep "cmdInitPhaseOp fallback"</verify>
  <done>`init phase-op` fallback path executes without `ReferenceError` and still returns legacy-compatible path/existence fields plus resolver metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run init compatibility suite to confirm no regressions</name>
  <files>tests/init.test.cjs</files>
  <action>Run the full init test suite after the import fix to confirm flat, hierarchical, and mixed layout behaviors remain unchanged, with special attention to fallback and resolver metadata scenarios established in Phase 4 plan 02.</action>
  <verify>npm test -- tests/init.test.cjs</verify>
  <done>All init tests pass, including fallback and compatibility cases, demonstrating the gap is closed without new regressions.</done>
</task>

</tasks>

<verification>
Run targeted fallback test and full `init` suite to prove the missing import is restored and resolver-backed init behavior remains stable across layouts.
</verification>

<success_criteria>
Phase 4 verification gap is resolved: `cmdInitPhaseOp` fallback no longer crashes, and init read-path workflows pass existing compatibility tests.
</success_criteria>

<output>
After completion, create `.planning/phases/04-hierarchy-layout-detection/04-04-SUMMARY.md`
</output>
