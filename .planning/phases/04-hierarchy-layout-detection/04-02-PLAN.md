---
phase: 04-hierarchy-layout-detection
plan: "02"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - get-shit-done/bin/lib/init.cjs
  - get-shit-done/bin/lib/commands.cjs
  - tests/init.test.cjs
  - tests/commands.test.cjs
autonomous: true
must_haves:
  truths:
    - "User can bootstrap hierarchical planning directories: `.planning/project/`, `.planning/workspace/current/`, `.planning/archive/milestones/`, and `.planning/todos/`."
    - "Init read-path outputs stay backward-compatible while resolver-backed."
    - "Progress command reads phase inventory correctly in flat and hierarchical layouts."
    - "Mixed/partial repos still work through hierarchical-preferred, flat-fallback resolution."
    - "Todos remain cross-milestone by resolving to `.planning/todos/` independently of workspace/current phase changes."
  artifacts:
    - path: "get-shit-done/bin/lib/init.cjs"
      provides: "Hierarchical directory bootstrap plus resolver-backed init payload paths with preserved legacy keys"
      contains: "state_path"
    - path: "get-shit-done/bin/lib/commands.cjs"
      provides: "Progress rendering that resolves phase roots via core resolver"
      contains: "cmdProgressRender"
    - path: "tests/init.test.cjs"
      provides: "Contract tests for init output compatibility and layout metadata"
      contains: "init progress"
    - path: "tests/commands.test.cjs"
      provides: "Progress tests for flat/hierarchical/mixed reads"
      contains: "describe('progress command'"
  key_links:
    - from: "get-shit-done/bin/lib/init.cjs"
      to: "get-shit-done/bin/lib/core.cjs"
      via: "init commands call resolver artifact APIs"
      pattern: "resolveArtifactPath|resolveLayout"
    - from: "get-shit-done/bin/lib/commands.cjs"
      to: "get-shit-done/bin/lib/core.cjs"
      via: "progress uses resolver-selected phases root"
      pattern: "cmdProgressRender[\\s\\S]*resolve"
---

<objective>
Migrate init and progress read paths to resolver outputs without breaking existing output contracts.

Purpose: Keep orchestration stable while enabling hierarchical workspace support.
Output: Resolver-backed init/progress command logic plus compatibility tests.
</objective>

<execution_context>
@/Users/lupickup/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/lupickup/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-hierarchy-layout-detection/04-RESEARCH.md
@.planning/phases/04-hierarchy-layout-detection/04-01-SUMMARY.md
@get-shit-done/bin/lib/init.cjs
@get-shit-done/bin/lib/commands.cjs
@tests/init.test.cjs
@tests/commands.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add hierarchical bootstrap + resolver-backed init path fields</name>
  <files>get-shit-done/bin/lib/init.cjs</files>
  <action>Implement an idempotent hierarchical bootstrap flow in init command paths that create `.planning/project/`, `.planning/workspace/current/`, `.planning/archive/milestones/`, and `.planning/todos/` when initializing a hierarchical workspace. Then refactor init read-path commands used in this phase (`init plan-phase`, `init execute-phase`, `init progress`, and shared phase lookup fields) to source existence/path values through resolver APIs instead of flat literals. Preserve legacy output keys and relative path semantics exactly (`*_path` and `*_exists`), and add only additive resolver metadata fields (layout mode/confidence/conflicts) where appropriate.</action>
  <verify>npm test -- tests/init.test.cjs</verify>
  <done>Hierarchical init creates all required directories exactly once (idempotent), and init outputs remain contract-compatible for flat repos while correctly reporting resolver-selected paths in hierarchical/mixed repos.</done>
</task>

<task type="auto">
  <name>Task 2: Resolver-back progress phase inventory reads</name>
  <files>get-shit-done/bin/lib/commands.cjs</files>
  <action>Update `cmdProgressRender` to resolve roadmap and phase directory roots via resolver APIs, including hierarchical-preferred selection and flat fallback. Keep existing rendered JSON/bar/table output shape unchanged, and ensure ambiguous/mixed layouts still produce deterministic totals/status values.</action>
  <verify>npm test -- tests/commands.test.cjs</verify>
  <done>Progress command renders valid output from resolver-selected roots in flat, hierarchical, and mixed layouts without schema drift.</done>
</task>

<task type="auto">
  <name>Task 3: Expand compatibility matrix tests for init/progress</name>
  <files>tests/init.test.cjs, tests/commands.test.cjs</files>
  <action>Add tests that run init/progress against flat, hierarchical, and mixed fixture structures. Include explicit bootstrap assertions that hierarchical initialization creates `.planning/project/`, `.planning/workspace/current/`, `.planning/archive/milestones/`, and `.planning/todos/` and is safe to run twice without changing outcomes. Add explicit todo stability assertions that resolver/init outputs keep todos anchored at `.planning/todos/` while simulated current milestone phase directories change under `.planning/workspace/current/phases`. Assert unchanged legacy keys and deterministic hierarchical-preferred behavior, including fallback/conflict signaling when both structures exist.</action>
  <verify>npm test -- tests/init.test.cjs tests/commands.test.cjs</verify>
  <done>Init/progress tests fail on missing hierarchical directory bootstrap, todo-root instability across milestone changes, contract regressions, missing resolver metadata, or incorrect layout precedence behavior.</done>
</task>

</tasks>

<verification>
Run init/progress test suites and verify hierarchical bootstrap directory creation, cross-milestone todos stability, and output contract preservation while sourcing paths from resolver decisions.
</verification>

<success_criteria>
Init supports hierarchical directory bootstrap (`project/`, `workspace/current/`, `archive/milestones/`, `todos/`), init/progress read paths in phase scope are resolver-backed and backward compatible, and tests validate flat/hierarchical/mixed behavior plus cross-milestone todos stability.
</success_criteria>

<output>
After completion, create `.planning/phases/04-hierarchy-layout-detection/04-02-SUMMARY.md`
</output>
