---
phase: 04-hierarchy-layout-detection
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/bin/lib/core.cjs
  - tests/core.test.cjs
autonomous: true
must_haves:
  truths:
    - "Layout resolution classifies workspace as none, flat, hierarchical, or ambiguous."
    - "Resolver reports confidence and conflict/fallback signals for mixed states."
    - "Phase/archive path selection is deterministic and prefers hierarchical roots."
    - "Todos resolve to `.planning/todos/` as a cross-milestone root and do not drift with workspace/current switching."
  artifacts:
    - path: "get-shit-done/bin/lib/core.cjs"
      provides: "Canonical artifact registry and resolver APIs for layout/path resolution"
      contains: "resolveLayout"
    - path: "tests/core.test.cjs"
      provides: "Resolver behavior coverage for flat/hierarchical/ambiguous/none and archive drift"
      contains: "describe('layout resolver'"
  key_links:
    - from: "get-shit-done/bin/lib/core.cjs"
      to: "get-shit-done/bin/lib/core.cjs"
      via: "findPhaseInternal uses resolver-selected phase roots"
      pattern: "findPhaseInternal[\\s\\S]*resolve"
    - from: "get-shit-done/bin/lib/core.cjs"
      to: "get-shit-done/bin/lib/core.cjs"
      via: "artifact registry maps archive namespace aliases"
      pattern: "archive/milestones|milestones"
---

<objective>
Establish a resolver-first core contract that detects workspace layout and resolves canonical planning artifact paths with deterministic fallback behavior.

Purpose: Make all downstream commands consume one source of truth for flat/hierarchical/mixed repositories.
Output: New resolver APIs and test coverage in core utilities.
</objective>

<execution_context>
@/Users/lupickup/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/lupickup/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-hierarchy-layout-detection/04-RESEARCH.md
@get-shit-done/bin/lib/core.cjs
@tests/core.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build canonical resolver APIs in core</name>
  <files>get-shit-done/bin/lib/core.cjs</files>
  <action>Create a canonical artifact registry for project/workspace/archive/todos paths with hierarchical canonical paths and flat fallbacks. Implement `resolveLayout(cwd)` returning `mode` (`none|flat|hierarchical|ambiguous`), confidence, and explicit signals (hierarchical hits, flat hits, conflicts). Implement `resolveArtifactPath(cwd, artifact)` that applies hierarchical-preferred + flat-fallback deterministically and returns metadata (`source`, `fallbackUsed`, `conflict`). Keep behavior read-only in this phase (no migration writes).</action>
  <verify>npm test -- tests/core.test.cjs</verify>
  <done>Core exposes resolver APIs that classify layout modes and return deterministic artifact paths with conflict/fallback metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize phase/archive roots behind resolver</name>
  <files>get-shit-done/bin/lib/core.cjs</files>
  <action>Refactor `findPhaseInternal` and `getArchivedPhaseDirs` to consume resolver-provided phase/archive roots instead of hardcoded `.planning/phases` and `.planning/milestones` literals. Normalize archive namespace drift by supporting canonical `.planning/archive/milestones` while accepting legacy `.planning/milestones` as fallback, and emit conflict signals when both exist.</action>
  <verify>npm test -- tests/core.test.cjs</verify>
  <done>Phase lookup and archived phase enumeration work through resolver roots and handle both canonical and legacy archive namespaces deterministically.</done>
</task>

<task type="auto">
  <name>Task 3: Add resolver mode and drift regression tests</name>
  <files>tests/core.test.cjs</files>
  <action>Add tests covering `none`, flat-only, hierarchical-only, and mixed/ambiguous layouts; assert confidence/conflict signal presence and deterministic hierarchical preference. Add archive namespace drift tests that validate canonical-first resolution with legacy fallback. Add explicit todos-path tests asserting resolver always returns `.planning/todos/` as a stable cross-milestone root regardless of active workspace/current fixture changes, with no regressions to existing helper exports/contracts.</action>
  <verify>npm test -- tests/core.test.cjs</verify>
  <done>Core tests fail if layout detection, confidence signaling, archive namespace normalization, or cross-milestone todos path stability regresses.</done>
</task>

</tasks>

<verification>
Run resolver-focused tests and confirm all new APIs are exported and consumed by existing phase/archive helper paths without breaking legacy behaviors, including stable todos-root resolution.
</verification>

<success_criteria>
`core.cjs` provides resolver-first layout/path APIs, detects all required layout states, normalizes archive namespace drift, and preserves cross-milestone todos-root stability with deterministic behavior proven by tests.
</success_criteria>

<output>
After completion, create `.planning/phases/04-hierarchy-layout-detection/04-01-SUMMARY.md`
</output>
