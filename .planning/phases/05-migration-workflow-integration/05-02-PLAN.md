---
phase: 05-migration-workflow-integration
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - get-shit-done/bin/lib/init.cjs
  - get-shit-done/bin/lib/milestone.cjs
  - get-shit-done/bin/lib/verify.cjs
  - get-shit-done/bin/lib/commands.cjs
  - tests/init.test.cjs
  - tests/milestone.test.cjs
  - tests/verify-health.test.cjs
autonomous: true
must_haves:
  truths:
    - "Write-path workflow internals use resolver-selected canonical targets instead of flat literals."
    - "Milestone/archive writes target canonical archive roots with flat fallback compatibility."
    - "Health/consistency checks remain correct in flat, hierarchical, and mixed layouts."
  artifacts:
    - path: "get-shit-done/bin/lib/init.cjs"
      provides: "Resolver-backed init payloads for all workflow entrypoints"
      contains: "resolveArtifactPath"
    - path: "get-shit-done/bin/lib/milestone.cjs"
      provides: "Resolver-aware milestone archive and requirements/state updates"
      contains: "archive"
    - path: "get-shit-done/bin/lib/verify.cjs"
      provides: "Layout-aware health and consistency checks"
      contains: "resolveArtifactPath"
    - path: "tests/init.test.cjs"
      provides: "Init contract coverage for hierarchical and mixed write roots"
      contains: "layout"
  key_links:
    - from: "get-shit-done/bin/lib/init.cjs"
      to: "get-shit-done/bin/lib/core.cjs"
      via: "init path fields come from resolver artifacts"
      pattern: "resolveArtifactPath"
    - from: "get-shit-done/bin/lib/milestone.cjs"
      to: "get-shit-done/bin/lib/core.cjs"
      via: "archive and phase roots resolved through artifact registry"
      pattern: "archiveMilestonesDir|resolveArtifactPath"
---

<objective>
Migrate write-path CLI internals to resolver-selected artifact paths so command behavior stays consistent across flat, hierarchical, and mixed repositories.

Purpose: Deliver the core code portion of WORK-01 and prevent split-brain writes after migration.
Output: Resolver-backed init/milestone/verify/commands modules with compatibility tests.
</objective>

<execution_context>
@/Users/lupickup/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/lupickup/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-migration-workflow-integration/05-RESEARCH.md
@get-shit-done/bin/lib/init.cjs
@get-shit-done/bin/lib/milestone.cjs
@get-shit-done/bin/lib/verify.cjs
@get-shit-done/bin/lib/commands.cjs
@tests/init.test.cjs
@tests/milestone.test.cjs
@tests/verify-health.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolver-back remaining init payload write/read paths</name>
  <files>get-shit-done/bin/lib/init.cjs</files>
  <action>Replace remaining flat-coupled path literals in init outputs (`resume`, `milestone-op`, `quick`, `map-codebase`, and other non-migrated payloads) with resolver-derived artifact roots where applicable. Preserve existing output keys to avoid orchestration contract breaks while updating path values to canonical hierarchical targets in mixed/hierarchical layouts.</action>
  <verify>npm test -- tests/init.test.cjs</verify>
  <done>Init payloads consistently report resolver-selected paths without breaking legacy field names.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate milestone and health/consistency write roots to resolver</name>
  <files>get-shit-done/bin/lib/milestone.cjs, get-shit-done/bin/lib/verify.cjs, get-shit-done/bin/lib/commands.cjs</files>
  <action>Refactor milestone archive targets, requirement/state/roadmap references, and health/consistency root discovery to use resolver artifacts (`roadmapFile`, `requirementsFile`, `stateFile`, `phasesDir`, `archiveMilestonesDir`). Remove hardcoded `.planning/milestones` assumptions in write logic while preserving flat fallback behavior.</action>
  <verify>npm test -- tests/milestone.test.cjs tests/verify-health.test.cjs tests/commands.test.cjs</verify>
  <done>Milestone and verification commands write/read through resolver-selected roots and remain backward compatible in flat layout repositories.</done>
</task>

<task type="auto">
  <name>Task 3: Expand compatibility matrix tests for CLI write-path modules</name>
  <files>tests/init.test.cjs, tests/milestone.test.cjs, tests/verify-health.test.cjs</files>
  <action>Add and adjust tests for flat, hierarchical, and mixed states covering init path fields, milestone archive destinations, and health checks. Include regression cases preventing writes to both canonical and legacy paths in the same run unless explicitly conflict-reporting.</action>
  <verify>npm test -- tests/init.test.cjs tests/milestone.test.cjs tests/verify-health.test.cjs</verify>
  <done>Test suites catch write-path drift and enforce canonical-preferred behavior with legacy compatibility.</done>
</task>

</tasks>

<verification>
Run init, milestone, verify-health, and commands test suites to confirm resolver-backed write/read behavior and compatibility across all supported layouts.
</verification>

<success_criteria>
WORK-01 core command modules are resolver-driven for write paths and produce deterministic canonical results without regressing flat repositories.
</success_criteria>

<output>
After completion, create `.planning/phases/05-migration-workflow-integration/05-02-SUMMARY.md`
</output>
