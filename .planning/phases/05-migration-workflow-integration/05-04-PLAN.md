---
phase: 05-migration-workflow-integration
plan: "04"
type: execute
wave: 3
depends_on:
  - "05-02"
files_modified:
  - get-shit-done/bin/lib/commands.cjs
  - get-shit-done/bin/lib/verify.cjs
  - get-shit-done/bin/lib/frontmatter.cjs
  - tests/frontmatter.test.cjs
  - tests/verify.test.cjs
  - tests/dispatcher.test.cjs
autonomous: true
must_haves:
  truths:
    - "A machine-parseable decisions queue can be created and maintained under `.planning/project/DECISIONS.md`."
    - "Contract files under `.planning/project/contracts/` can be scaffolded with required metadata fields."
    - "Verification can detect missing/invalid contract references used by planning artifacts."
  artifacts:
    - path: "get-shit-done/bin/lib/commands.cjs"
      provides: "Scaffold/create helpers for decisions queue and contract files"
      contains: "DECISIONS.md"
    - path: "get-shit-done/bin/lib/verify.cjs"
      provides: "Contract validation checks tied to plan/verification artifacts"
      contains: "contract"
    - path: "get-shit-done/bin/lib/frontmatter.cjs"
      provides: "Parser support for decision/contract metadata fields"
      contains: "extractFrontmatter"
  key_links:
    - from: "get-shit-done/bin/lib/verify.cjs"
      to: ".planning/project/contracts"
      via: "verification resolves contract files from project root"
      pattern: "contracts"
    - from: "get-shit-done/bin/lib/commands.cjs"
      to: ".planning/project/DECISIONS.md"
      via: "scaffold command writes machine-parseable decision queue"
      pattern: "DECISIONS.md"
---

<objective>
Introduce machine-parseable decision and contract primitives, plus verification hooks that enforce their required structure and references.

Purpose: Deliver DEC-01, DEC-02, CONT-01, and CONT-02 as durable governance artifacts for parallel execution.
Output: Scaffolding/validation support for `DECISIONS.md` and project contracts with regression tests.
</objective>

<execution_context>
@/Users/lupickup/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/lupickup/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-migration-workflow-integration/05-RESEARCH.md
@.planning/workspace/current/DESIGN-workspace-enhancements.md
@get-shit-done/bin/lib/commands.cjs
@get-shit-done/bin/lib/verify.cjs
@get-shit-done/bin/lib/frontmatter.cjs
@tests/frontmatter.test.cjs
@tests/verify.test.cjs
@tests/dispatcher.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add scaffold support for decisions queue and contracts</name>
  <files>get-shit-done/bin/lib/commands.cjs, tests/dispatcher.test.cjs</files>
  <action>Extend scaffold utilities with deterministic templates for `.planning/project/DECISIONS.md` and `.planning/project/contracts/CONTRACT-*.md` including required metadata (`status`, source links, options/rationale/impact for decisions; owner/consumers/invariants/version for contracts). Ensure command routing and error messages are covered by dispatcher tests.</action>
  <verify>npm test -- tests/dispatcher.test.cjs</verify>
  <done>CLI can create machine-parseable decisions queue and contract files in canonical project roots.</done>
</task>

<task type="auto">
  <name>Task 2: Add parser and verification rules for decision/contract metadata</name>
  <files>get-shit-done/bin/lib/frontmatter.cjs, get-shit-done/bin/lib/verify.cjs</files>
  <action>Implement metadata validation helpers for decisions and contracts, then add verify checks that detect malformed statuses/links and missing contract files when plans declare contract dependencies. Keep checks additive and non-breaking for existing plan schemas.</action>
  <verify>npm test -- tests/frontmatter.test.cjs tests/verify.test.cjs</verify>
  <done>Verification reports actionable errors for invalid decision/contract artifacts and passes for valid machine-parseable files.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression tests for DEC/CONT artifact contracts</name>
  <files>tests/frontmatter.test.cjs, tests/verify.test.cjs</files>
  <action>Add focused tests covering required decision statuses (`pending|decided|revisit`), required source linkage fields (phase/plan/requirements), contract version/invariant fields, and violation detection when referenced contracts are missing or breaking without version bump metadata.</action>
  <verify>npm test -- tests/frontmatter.test.cjs tests/verify.test.cjs</verify>
  <done>Test coverage protects DEC/CONT schemas and contract-reference verification semantics.</done>
</task>

</tasks>

<verification>
Scaffold new decision/contract artifacts in a temp project and run parser/verify tests to confirm machine-parseability and violation detection.
</verification>

<success_criteria>
DEC-01, DEC-02, CONT-01, and CONT-02 are implemented with structured artifact templates and enforceable verification checks.
</success_criteria>

<output>
After completion, create `.planning/phases/05-migration-workflow-integration/05-04-SUMMARY.md`
</output>
