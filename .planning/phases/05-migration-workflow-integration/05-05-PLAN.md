---
phase: 05-migration-workflow-integration
plan: "05"
type: execute
wave: 4
depends_on:
  - "05-03"
  - "05-04"
files_modified:
  - get-shit-done/bin/lib/commands.cjs
  - get-shit-done/bin/lib/milestone.cjs
  - get-shit-done/workflows/execute-phase.md
  - get-shit-done/workflows/verify-work.md
  - get-shit-done/workflows/complete-milestone.md
  - get-shit-done/templates/config.json
  - tests/milestone.test.cjs
  - tests/commands.test.cjs
autonomous: true
must_haves:
  truths:
    - "Spec delta artifacts are generated under `.planning/workspace/current/spec-deltas/` for completed plans/phases."
    - "Accepted deltas can be propagated into canonical specs with an auditable trail."
    - "Nested planning artifact filtering rules include hierarchical paths for PR branch hygiene."
  artifacts:
    - path: "get-shit-done/bin/lib/commands.cjs"
      provides: "Spec-delta generation and apply/propagation helpers"
      contains: "spec-deltas"
    - path: "get-shit-done/bin/lib/milestone.cjs"
      provides: "Milestone-close reconciliation for open deltas"
      contains: "delta"
    - path: "get-shit-done/templates/config.json"
      provides: "Default nested planning filter path patterns"
      contains: "**"
  key_links:
    - from: "get-shit-done/workflows/execute-phase.md"
      to: ".planning/workspace/current/spec-deltas"
      via: "post-plan/phase completion emits delta artifact"
      pattern: "spec-deltas"
    - from: "get-shit-done/bin/lib/commands.cjs"
      to: ".planning/project/PROJECT.md"
      via: "accepted delta propagation patches canonical specs"
      pattern: "PROJECT.md|REQUIREMENTS.md|ROADMAP.md"
---

<objective>
Implement spec-delta generation and propagation workflows, and lock nested planning path filters for PR hygiene in hierarchical layouts.

Purpose: Deliver DELTA-01, DELTA-02, and WORK-03 closure for migration-integrated workflows.
Output: Spec-delta artifact pipeline, propagation tooling/audit trail, and hierarchical nested filter defaults with tests.
</objective>

<execution_context>
@/Users/lupickup/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/lupickup/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-migration-workflow-integration/05-RESEARCH.md
@.planning/workspace/current/DESIGN-workspace-enhancements.md
@get-shit-done/bin/lib/commands.cjs
@get-shit-done/bin/lib/milestone.cjs
@get-shit-done/workflows/execute-phase.md
@get-shit-done/workflows/verify-work.md
@get-shit-done/workflows/complete-milestone.md
@get-shit-done/templates/config.json
@tests/milestone.test.cjs
@tests/commands.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate structured spec-delta artifacts during execution lifecycle</name>
  <files>get-shit-done/bin/lib/commands.cjs, get-shit-done/workflows/execute-phase.md, get-shit-done/workflows/verify-work.md</files>
  <action>Add deterministic delta creation logic and workflow hooks so completed plans/phases write files under `.planning/workspace/current/spec-deltas/` with required metadata (`id`, scope, phase/plan, status, linked decisions, timestamps). Ensure generated files are machine-parseable and linked from execution/verification outputs.</action>
  <verify>npm test -- tests/commands.test.cjs</verify>
  <done>Phase execution consistently emits parseable spec-delta artifacts with stable naming and metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Implement accepted-delta propagation with audit trail</name>
  <files>get-shit-done/bin/lib/commands.cjs, get-shit-done/bin/lib/milestone.cjs, get-shit-done/workflows/complete-milestone.md, tests/milestone.test.cjs</files>
  <action>Add a propagation path that applies accepted deltas into canonical specs (`PROJECT.md`, `REQUIREMENTS.md`, `ROADMAP.md`) and records audit metadata (source delta id, applied timestamp, touched files). Update milestone-complete reconciliation to report unresolved/open deltas distinctly from applied ones.</action>
  <verify>npm test -- tests/milestone.test.cjs tests/commands.test.cjs</verify>
  <done>Accepted deltas are traceably applied to canonical specs, and unresolved deltas remain visible at milestone close.</done>
</task>

<task type="auto">
  <name>Task 3: Enforce nested hierarchical planning path filter defaults</name>
  <files>get-shit-done/templates/config.json, tests/commands.test.cjs</files>
  <action>Update default filter path configuration to explicitly include nested hierarchical planning artifacts (recursive `.planning/**` patterns plus canonical workspace/project/archive roots) so PR filtering remains effective after migration. Add tests that assert nested planning files are matched by default filter patterns.</action>
  <verify>npm test -- tests/commands.test.cjs</verify>
  <done>Default filtering rules cover nested hierarchical planning paths and regression tests lock the behavior.</done>
</task>

</tasks>

<verification>
Run command and milestone tests, then execute a sample plan/phase completion flow to confirm delta files are produced, propagated, and audited under canonical workspace roots.
</verification>

<success_criteria>
DELTA-01 and DELTA-02 are implemented end-to-end, and WORK-03 nested planning-path filtering behavior is explicitly codified in defaults and tests.
</success_criteria>

<output>
After completion, create `.planning/phases/05-migration-workflow-integration/05-05-SUMMARY.md`
</output>
