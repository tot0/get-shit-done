---
phase: 05-migration-workflow-integration
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/bin/lib/core.cjs
  - get-shit-done/bin/gsd-tools.cjs
  - tests/core.test.cjs
  - tests/dispatcher.test.cjs
autonomous: true
must_haves:
  truths:
    - "User can run one migration command to map flat planning artifacts into hierarchical roots."
    - "Running migration repeatedly is idempotent and reports noop/moved/conflict operations deterministically."
    - "Migration never overwrites when both flat and canonical artifacts already exist."
  artifacts:
    - path: "get-shit-done/bin/lib/core.cjs"
      provides: "Migration mapping engine and operation report statuses"
      contains: "migrate"
    - path: "get-shit-done/bin/gsd-tools.cjs"
      provides: "CLI command surface for migration"
      contains: "migrate"
    - path: "tests/core.test.cjs"
      provides: "Migration behavior coverage for flat, mixed, rerun scenarios"
      contains: "migration"
    - path: "tests/dispatcher.test.cjs"
      provides: "Dispatcher route coverage for migration command"
      contains: "migrate"
  key_links:
    - from: "get-shit-done/bin/gsd-tools.cjs"
      to: "get-shit-done/bin/lib/core.cjs"
      via: "CLI dispatch invokes migration engine"
      pattern: "migrate"
    - from: "get-shit-done/bin/lib/core.cjs"
      to: ".planning/workspace/current"
      via: "artifact mapping from flat to canonical targets"
      pattern: "workspace/current|project/PROJECT.md|archive/milestones"
---

<objective>
Add an idempotent migration command and shared mapping contract that safely moves flat planning artifacts into the hierarchical workspace layout.

Purpose: Deliver MIGR-01 and MIGR-03 with deterministic behavior before broader write-path migration.
Output: Core migration API, CLI entrypoint, and regression tests for moved/noop/conflict outcomes.
</objective>

<execution_context>
@/Users/lupickup/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/lupickup/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-migration-workflow-integration/05-RESEARCH.md
@.planning/workspace/current/DESIGN-workspace-enhancements.md
@get-shit-done/bin/lib/core.cjs
@get-shit-done/bin/gsd-tools.cjs
@tests/core.test.cjs
@tests/dispatcher.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement idempotent flat-to-hierarchical migration engine</name>
  <files>get-shit-done/bin/lib/core.cjs</files>
  <action>Add a single migration helper in core that maps `PROJECT.md` into `project/`, milestone-scoped docs and phase/research roots into `workspace/current/`, and preserves `.planning/todos` in place. Return a structured report per artifact with `moved|copied|noop|conflict` status. Do not overwrite canonical files when both flat and hierarchical artifacts exist; emit conflicts instead.</action>
  <verify>npm test -- tests/core.test.cjs</verify>
  <done>Core exposes a deterministic migration API that can be re-run safely and reports exact operations/conflicts.</done>
</task>

<task type="auto">
  <name>Task 2: Expose migration through gsd-tools CLI</name>
  <files>get-shit-done/bin/gsd-tools.cjs</files>
  <action>Add a top-level migration command route and help text that invokes the new core migration helper, returning structured JSON suitable for orchestration (`changed`, `mode_before`, `mode_after`, `operations`, `conflicts`, `warnings`). Preserve existing command behavior and error formats for unrelated commands.</action>
  <verify>node get-shit-done/bin/gsd-tools.cjs migrate layout --raw</verify>
  <done>CLI supports one migration command that executes without destructive overwrite and outputs structured results.</done>
</task>

<task type="auto">
  <name>Task 3: Add migration regression and dispatcher coverage</name>
  <files>tests/core.test.cjs, tests/dispatcher.test.cjs</files>
  <action>Add tests covering flat-only migration success, mixed-layout conflict reporting, and second-run idempotency (`noop` dominant). Add dispatcher coverage for new migration routing and unknown migrate subcommand error handling.</action>
  <verify>npm test -- tests/core.test.cjs tests/dispatcher.test.cjs</verify>
  <done>Tests enforce migration contract and CLI routing behavior across success and conflict paths.</done>
</task>

</tasks>

<verification>
Run migration-focused tests and execute the migration command in a temp project fixture to confirm non-destructive behavior and idempotent re-runs.
</verification>

<success_criteria>
MIGR-01 and MIGR-03 are satisfied by a single command that maps artifacts correctly, preserves todos, and remains safe under repeated execution.
</success_criteria>

<output>
After completion, create `.planning/phases/05-migration-workflow-integration/05-01-SUMMARY.md`
</output>
